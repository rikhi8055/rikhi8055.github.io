{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"],
  "compilerOptions": {
    // to enable feature => import from "src/path.js"
    "baseUrl": ".",
    // You can use below paths also - but not required as we can import as "/src/../.."
    // "paths": {
    //   "@components/*": ["src/components/*"],
    //   "@assets/*": ["src/assets/*"]
    // }
  }
}
